# Frontend Fundamentals 모의고사 2회: 회의실 예약 리팩터링 과제 복원

## 목표

이미 동작하는 회의실 예약 서비스의 사용자 동작을 유지하면서, 코드를 더 읽기 쉽고 변경하기 쉬운 구조로 리팩터링합니다.

이 문서는 다음 두 내용을 구분해 정리합니다.

1. 회의실 예약 서비스가 만족해야 하는 **제품 기능 명세**
2. 2026년 3월 Frontend Fundamentals 모의고사 2회에서 수행한 **리팩터링 과제**

공식 제품 기능 명세는 저장소의 `docs/requirements.md`에 남아 있습니다. 반면 모의고사 행사 안내 Notion은 현재 공식 내용을 표시하지 않으므로, 리팩터링 지시는 공식 저장소·PR 템플릿과 여러 참가 후기를 교차 확인해 복원했습니다.

## 문서의 증거 수준

- **직접 확인**: `docs/requirements.md`, 완성된 시작 코드, 자동화 테스트, 공식 PR 템플릿
- **교차 확인**: 여러 참가 후기에 공통으로 기록된 리팩터링 목적과 해설 주제
- **불확실**: 행사 채널에서 전달된 리팩터링 지시문의 정확한 원문과 공식 평가 항목별 비중

따라서 제품 기능은 공식 명세로 볼 수 있지만, 리팩터링 관점은 공식 채점표가 아니라 공개 자료로 복원한 학습용 기준입니다.

## 행사에서 수행한 과제

- 2026년 3월 Frontend Fundamentals 모의고사 2회 과제입니다.
- 의도한 수행 시간은 2시간이었습니다.
- 새로운 기능을 처음부터 구현하는 과제가 아니라, 이미 기능이 동작하는 회의실 예약 코드를 개선하는 과제였습니다.
- 제공된 테스트를 회귀 방지 수단으로 사용하면서 외부에서 관찰되는 동작을 유지해야 했습니다.
- 제출 시 `리팩토링 PR`을 선택하고 어떤 부분에 초점을 맞췄는지 Pull Request에 설명하도록 공식 템플릿이 안내합니다.

공개 자료를 종합하면 실행 가능한 과제 문장은 다음과 같이 복원할 수 있습니다.

> 제공된 회의실 예약 서비스의 기능과 테스트를 유지하면서, UI와 코드의 대응 관계, 컴포넌트 책임, 상태와 데이터 흐름, 인터페이스의 예측 가능성을 개선하도록 리팩터링한다.

위 문장은 공개 자료를 바탕으로 재구성한 요약이며 공식 원문 인용이 아닙니다.

## 사전 준비

- Node.js `22.12.0` (`.nvmrc` 기준)
- Yarn `1.22.22` (`package.json` 기준)

```sh
yarn install
yarn start
```

리팩터링 전 기준 동작을 확인합니다.

```sh
yarn test
yarn build
```

## Part A. 제품 기능 명세

전체 공식 명세는 `docs/requirements.md`에서 확인합니다. 아래 내용은 리팩터링 과정에서 보존해야 하는 외부 동작을 빠르게 확인하기 위한 요약입니다.

### 1. 예약 현황 페이지 `/`

- 날짜를 선택합니다.
- 선택한 날짜의 회의실별 예약 현황을 타임라인으로 표시합니다.
- 타임라인은 `09:00`부터 `20:00`까지 30분 단위로 구성합니다.
- 예약 블록을 선택하면 시간, 참석 인원, 장비 정보를 표시합니다.
- 내 예약 목록에 날짜, 시간, 회의실명, 참석 인원, 장비를 표시합니다.
- 확인 대화상자를 거쳐 내 예약을 취소합니다.
- 취소 성공 후 내 예약 목록과 예약 현황을 모두 갱신합니다.
- `예약하기` 버튼으로 `/booking`에 이동합니다.

### 2. 예약하기 페이지 `/booking`

사용자가 다음 조건을 입력할 수 있어야 합니다.

- 날짜
- 시작 시간과 종료 시간
- 참석 인원
- 필요한 장비 복수 선택
- 선택 사항인 선호 층

다음 조건을 모두 만족하는 회의실만 예약 가능 목록에 표시합니다.

- `room.capacity >= attendees`
- 선택한 모든 장비가 `room.equipment`에 포함됨
- 선호 층이 있으면 `room.floor === preferredFloor`
- 같은 날짜와 회의실의 기존 예약이 요청한 `[start, end)` 구간과 겹치지 않음

목록은 층 오름차순, 같은 층에서는 이름순으로 정렬합니다.

### 3. 필터와 URL

- 필터 조건을 URL 쿼리 매개변수로 공유할 수 있어야 합니다.
- 날짜 기본값은 오늘입니다.
- 과거 날짜는 선택할 수 없습니다.
- 시작 시간은 `09:00`부터 `19:30`까지 선택할 수 있습니다.
- 종료 시간은 `09:30`부터 `20:00`까지 선택할 수 있습니다.
- 종료 시간은 시작 시간보다 늦어야 합니다.
- 참석 인원은 1명 이상이어야 합니다.
- 필터가 바뀌면 선택된 회의실과 기존 오류 메시지를 초기화합니다.
- 시간 조건이 완성되지 않았거나 유효성 오류가 있으면 예약 가능 목록을 표시하지 않습니다.

### 4. 예약 생성

- 예약 가능 목록에서 회의실 하나를 선택합니다.
- 선택한 회의실을 시각적으로 구분합니다.
- 회의실을 선택하지 않고 예약하면 안내 메시지를 표시합니다.
- 요청 중에는 버튼을 비활성화하고 진행 상태를 표시합니다.
- 성공하면 성공 메시지를 표시하고 예약 현황 페이지로 이동합니다.
- 성공 후 해당 날짜의 예약 현황을 다시 가져옵니다.
- 실패하면 서버 오류 메시지를 표시하고 회의실 선택을 초기화합니다.

### 5. 빈 상태와 개수

- 내 예약이 없을 때 빈 상태 메시지를 표시합니다.
- 조건에 맞는 회의실이 없을 때 빈 상태 메시지를 표시합니다.
- 예약 가능 회의실 개수와 내 예약 건수를 각 목록 헤더에 표시합니다.

## API 명세

```http
GET /api/rooms
GET /api/reservations?date=YYYY-MM-DD
GET /api/my-reservations
POST /api/reservations
DELETE /api/reservations/:id
```

API의 전체 요청·응답 타입과 오류 코드는 `docs/requirements.md`를 기준으로 합니다.

## Part B. 모의고사 리팩터링 요구사항

### 반드시 유지할 것

- `docs/requirements.md`에 정의된 사용자 동작
- API 요청의 의미와 성공·실패 처리
- URL로 공유되는 필터 상태
- 시간 충돌 계산의 `[start, end)` 규칙
- 기존 자동화 테스트가 검증하는 동작
- 접근 가능한 화면 상태와 사용자 메시지

### 개선 대상

시작 코드 기준으로 주요 페이지 두 개에 약 700줄의 UI, 서버 상태, 입력 상태, 계산, 오류 처리가 함께 배치돼 있습니다.

- `ReservationStatusPage/index.tsx`: 예약 현황 조회, 타임라인, 내 예약, 취소
- `RoomBookingPage/index.tsx`: URL 필터, 유효성 검증, 회의실 필터링, 예약 생성

파일 길이 자체는 결함의 원인이 아닙니다. 실제 개선 대상은 서로 다른 책임을 이해하려면 한 번에 많은 상태와 구현 세부사항을 기억해야 하는 구조입니다.

### 공개 해설 후기에서 반복적으로 확인되는 관점

다음 항목은 공식 채점표가 아니라, 여러 참가자가 해설에서 공통으로 기록한 관점입니다.

1. **요구사항과 UI를 먼저 본다**
   - 기존 구현에 바로 끌려가지 않고, 제품 요구사항과 화면에서 이상적인 인터페이스를 먼저 설계합니다.
2. **UI와 코드의 위치를 대응시킨다**
   - 화면에서 보이는 책임을 코드에서도 쉽게 찾을 수 있어야 합니다.
3. **구현 세부사항과 역할을 구분한다**
   - 시간 좌표 계산이나 타임라인 배치 같은 `어떻게`는 내부에 숨깁니다.
   - 예약 현황, 내 예약, 예약 조건처럼 화면이 수행하는 `무엇`은 호출부에서 드러냅니다.
4. **컴포넌트 관점의 인터페이스를 사용한다**
   - 부모의 상태 구현을 드러내는 `setDate`보다 컴포넌트 역할을 표현하는 `value`와 `onChange`를 사용합니다.
5. **관련 코드를 가까이 둔다**
   - 재사용되지 않는 로직을 무조건 별도 디렉터리로 옮겨 시점 이동을 늘리지 않습니다.
6. **추출과 추상화를 구분한다**
   - 파일을 나누는 행위만으로 책임이 분리됐다고 판단하지 않습니다.
   - 호출부가 알아야 할 맥락을 줄이는 인터페이스가 만들어졌는지 확인합니다.

## 권장 리팩터링 순서

다음 순서는 공개 해설 관점을 실습하기 위한 제안이며 공식 필수 순서는 아닙니다.

1. 모든 테스트와 빌드가 통과하는 기준 상태를 기록합니다.
2. `docs/requirements.md`와 실제 화면을 비교해 보존할 동작을 체크리스트로 만듭니다.
3. 기존 페이지 코드를 복사하지 않은 빈 문서에서 이상적인 화면 구조와 컴포넌트 인터페이스를 먼저 적습니다.
4. 예약 현황, 내 예약, 예약 조건, 예약 가능 목록의 책임을 하나씩 분리합니다.
5. 시간 계산·필터링·정렬처럼 입력과 출력이 명확한 계산을 UI 부수 효과와 분리합니다.
6. URL 상태와 중복되는 로컬 상태가 있는지 확인하고 진실 공급원을 명확히 합니다.
7. 한 번에 한 책임만 옮기고 매 단계마다 테스트를 실행합니다.
8. 최종 화면과 API 호출이 기준 상태와 동일한지 수동으로 검증합니다.

## 최종 결과 검증

```sh
yarn test
yarn build
```

- [ ] 모든 기존 테스트가 통과합니다.
- [ ] 날짜를 변경하면 해당 날짜의 예약 현황이 갱신됩니다.
- [ ] URL을 복사해 열면 예약 필터가 복원됩니다.
- [ ] 회의실 수용 인원·장비·층·시간 충돌 필터가 유지됩니다.
- [ ] 예약 성공·실패·로딩 상태가 유지됩니다.
- [ ] 예약 취소 후 두 목록이 함께 갱신됩니다.
- [ ] 페이지에서 각 UI 영역의 코드를 빠르게 찾을 수 있습니다.
- [ ] 컴포넌트의 공개 인터페이스가 호출부의 상태 관리 구현에 종속되지 않습니다.
- [ ] 분리한 파일을 이해하기 위해 불필요한 시점 이동이 늘어나지 않았습니다.
- [ ] Pull Request에 리팩터링 목적과 선택 근거를 설명했습니다.

## 출처

- [공식 GitHub 저장소](https://github.com/toss-fe-interview/frontend-fundamentals-mock-exam-2603)
- [공식 제품 요구사항](https://github.com/toss-fe-interview/frontend-fundamentals-mock-exam-2603/blob/main/docs/requirements.md)
- [공식 Pull Request 템플릿](https://github.com/toss-fe-interview/frontend-fundamentals-mock-exam-2603/blob/main/.github/pull_request_template.md)
- [이미 동작하는 코드 개선 과제로 기록한 참가 후기](https://jgjgill.com/post/toss-frontend-fundamentals-mock-exam-2-review/)
- [2시간 리팩터링 과제와 해설을 정리한 참가 후기](https://pestudent.tistory.com/104)
- [리팩터링 목적과 해설 관점을 정리한 참가 후기](https://agetbase.com/blog/frontend-fundamentals-mock-exam-2/)
- 삭제된 공식 모집 문서의 이전 주소: `https://tosspublic.notion.site/frontend-fundamentals-mock-exam-notice`

마지막 출처는 2026년 8월 24일 기준으로 공식 내용을 표시하지 않습니다.
